import ExecutiveSection from './_components/sections/ExecutiveSection'

/** The console's landing page. The shell — sidebar, header, palette — is in layout.tsx. */
export default function Page() {
  return <ExecutiveSection />
}
